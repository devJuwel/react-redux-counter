1. create a service folder
2. > create a constants folder > and create constants file > add all types name
3. > create a Action folder > create action
4. > create a resources folder > create resources
5. src folder > create store.jsx file > create store
6. index.jsx file > add redux` <provider store={store.jsx}></provider>` components as parents component
7. use store own components with redux hook cont stateName = useSelector(state => state.count);
